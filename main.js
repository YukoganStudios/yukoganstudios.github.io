function reverse(str) {
    array = Array.from(str);
    //console.log(array);

    var result = '';
    for (i = array.length - 1;i >= 0; i --) {
        //console.log(array[i]);
        document.getElementById("str").innerHTML = 'yaguyeh';
        //console.log(ar6 + ar5 + ar4 + ar3 + ar2 + ar1)
        //result = result + array[i];
        //result = 'yaguyeh';
    }
    return result;
}
